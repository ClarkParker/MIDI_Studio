# Parked

An earlier pass jumped from the analysis straight to a solution — a seeded
"Drift" performer stage with six new parameters, plus DSP/UI specs and a reply
to the maker built on it. That direction was chosen before the product goal was
agreed, so it is parked here rather than deleted.

Parts of it may return later as polish *inside* a playing style (see the
concept, §7 and §9). Do not send `06_REPLY_TO_MAKER_drift-seed.md` to the beta
group; it argues for the parked direction.

The analysis it was based on (`../01_ALPHA_ANALYSIS.md`) and the platform
constraints (`../03_PLATFORM_NOTES.md` §2–§5) remain valid.
